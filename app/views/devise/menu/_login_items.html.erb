<% if user_signed_in? %>
  <%= link_to('Logout', destroy_user_session_path, method: :delete, class: 'logout') %>
  <div class="btn-group">
    <button type="button" class="btn btn-default dropdown-toggle MyAccount" data-toggle="dropdown">
      MyAccount <span class="caret"></span>
    </button>
    <ul class="dropdown-menu" role="menu">
      <li><%= link_to 'Veiw orders',veiw_order_user_orders_path(current_user),class: 'veiw_order'%></li>
      <li><%= link_to 'Profile',profile_users_path, class: 'profile' %></li>
    </ul>
  </div>
<% end %>          